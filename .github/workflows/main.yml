on: [push, pull_request]
jobs:
  maanam:
    steps: 
      - run: echo Hello World
    runs-on: ${{matrix.platfirm}}    
    strategy:
      matrix: 
        platform: [macos-latest,windows-latest, ubuntu-latest]
